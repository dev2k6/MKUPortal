name = "mku-crash-reporter"
main = "worker.js"
compatibility_date = "2024-09-01"

# Environment variables that can be committed (non-sensitive)
[vars]
ENVIRONMENT = "production"

# Secret variables must be set via wrangler CLI or Cloudflare Dashboard:
# 1. npx wrangler secret put TELEGRAM_BOT_TOKEN
# 2. npx wrangler secret put TELEGRAM_CHAT_ID
# 3. (Optional) npx wrangler secret put API_SECRET_KEY
